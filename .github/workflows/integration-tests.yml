name: Docker Image CI

on: [push]

jobs:

  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run Integration Tests
      run: docker-compose up -d --exit-code-from integration-tests integration-tests
